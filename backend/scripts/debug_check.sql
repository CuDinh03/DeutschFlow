-- Check all classes
SELECT id, name, teacher_id FROM teacher_classes;

-- Check students in the first class
SELECT * FROM class_students;

-- Check assignments in the first class
SELECT * FROM class_assignments;

-- Check student assignments
SELECT * FROM student_assignments;
