# DeutschFlow — Mô hình B2B (ĐÃ CHỐT)

> **Ngày:** 2026-06-21 · Đây là **nguồn sự thật** cho việc hoàn chỉnh B2B. Mọi plan/thi công bám đúng doc này, KHÔNG tự đoán. Khác doc cũ → doc này thắng.
> Nền code hiện tại: `User.Role = {STUDENT, TEACHER, ADMIN}` (danh tính toàn cục) · `OrgMember.role` (thành viên tổ chức) · entity `Organization/OrgMember(Id)/OrgInvitation/OrgInvoice/OrgPaymentEvent`. Materials **chưa có bảng** (controller chỉ sinh PPTX, không lưu).

---

## 1. Hai tầng, ba scope quyền (sửa xung đột tên)

Đổi org-role `ADMIN` → **`MANAGER`**. Sau đổi:
- **PLATFORM-ADMIN** = `User.Role.ADMIN` (= chủ nền tảng / "tôi"). Chỉ chữ này mới nghĩa "admin".
- **ORG roles** (`OrgMember.role`) = **`OWNER` · `MANAGER` · `TEACHER` · `STUDENT`**.
  - OWNER = tài khoản trung tâm. MANAGER = quản lý được OWNER uỷ quyền trong TT.

Nguyên tắc cốt lõi: **tách DANH TÍNH (`User`, portable) khỏi THÀNH VIÊN (`OrgMember`)**. Giáo viên rời TT = xoá/đóng membership; `User` còn nguyên → tự thành "giáo viên tự do".

---

## 2. Quyết định đã chốt

1. **1 giáo viên — 1 trung tâm tại một thời điểm** (1 `OrgMember` status=ACTIVE). Schema cho phép nhiều dòng (lịch sử/tương lai) nhưng enforce 1 ACTIVE.
2. **Rời TT:** lớp + học viên + **tài liệu của trung tâm** thuộc **trung tâm**; **hồ sơ giảng dạy + tài liệu cá nhân** theo **giáo viên**.
3. **Admin cấp tài khoản trung tâm = PLATFORM-ADMIN pre-create tài khoản OWNER** (top-down). **Thay** quyết định cũ "mời Owner tự đăng ký". Owner **vẫn sở hữu** account (danh tính person-owned & portable — *provision ≠ own*, xem §2.2). *(⚠️ Code hiện CHƯA pre-create OWNER — vẫn invite/attach; xem §6 "còn thiếu".)*
4. **"Giáo viên tự do" = suy ra** (`User(TEACHER)` không có `OrgMember` ACTIVE) — KHÔNG thêm cờ/bảng.

---

## 2.1 Provisioning tài khoản TOP-DOWN (CHỐT 2026-06-22)

**Chuỗi cấp tài khoản (pre-create, top-down):**
- **PLATFORM-ADMIN** pre-create tài khoản **OWNER** (tài khoản trung tâm).
- **OWNER** pre-create **MANAGER · TEACHER · STUDENT** của trung tâm.
- **MANAGER** pre-create **TEACHER · STUDENT** — **KHÔNG** tạo MANAGER (tạo/đổi MANAGER là **OWNER-only**, khớp `changeRole` owner-only đã có ở #143).

**Tài khoản GIÁO VIÊN — NOW vs FUTURE:**
- **NOW (Phase 1):** TEACHER do MANAGER/OWNER **cấp** (pre-create). *(Đang thi công — xem §6.)*
- **FUTURE (Phase 2 — ĐỊNH HƯỚNG, CHƯA thi công):** TEACHER **tự đăng ký** (self-signup) như STUDENT. **Chưa làm** — chưa có endpoint self-signup giáo viên. *(Đánh dấu rõ để không tưởng đã có.)*

## 2.2 Quy tắc SỞ HỮU khi pre-create (KHÔNG được phá portability)

1. **"Tạo tài khoản" = provisioning (cấp/onboard), KHÔNG phải sở hữu danh tính.**
2. **TEACHER do OWNER/MANAGER tạo VẪN là danh tính person-owned & PORTABLE:** rời TT = đóng membership (status REVOKED/LEFT), **account vẫn sống** → tự thành giáo viên tự do. **OWNER/MANAGER KHÔNG xoá/sửa được danh tính** (mật khẩu/email/xoá account) của giáo viên.
3. **Lock/suspend account = quyền PLATFORM-ADMIN** (vận hành nền tảng: chống lạm dụng / nợ phí). **Reversible** (`users.is_active`), **KHÔNG phải** org sở hữu account, **KHÔNG** = xoá danh tính.
4. **STUDENT do OWNER/MANAGER tạo = roster của trung tâm** (đếm seat, gắn TT).

**Provenance `created_via`** *(thiết kế để KHÔNG rework khi mở self-signup):* thêm cột `created_via ∈ {ADMIN, OWNER, MANAGER, SELF}` chỉ ghi **NGUỒN TẠO**, **KHÔNG** ảnh hưởng sở hữu/quyền. Mở self-signup (Phase 2) = thêm endpoint ghi `created_via=SELF`, **không migrate mô hình**. *(Hiện CHƯA có cột — xem §6.)*

---

## 3. Ma trận quyền (chốt)

| Hành động | PLATFORM-ADMIN | OWNER | MANAGER | TEACHER |
|---|---|---|---|---|
| Pre-create **OWNER** (tài khoản trung tâm) | ✅ | — | — | — |
| Pre-create **TEACHER / STUDENT** (nhân sự TT) | ✅ | ✅ | ✅ | — |
| Pre-create **MANAGER** | ✅ | ✅ | ❌ (OWNER-only) | — |
| Xem giáo viên **tự do** | ✅ | — | — | — |
| Xem chi tiết giáo viên **thuộc TT** | ❌ mặc định (break-glass + audit log) | ✅ trong TT | ✅ trong TT | — |
| Mời/gỡ membership giáo viên | — | ✅ | ✅ (giới hạn) | tự rời |
| **Sửa/xoá DANH TÍNH** giáo viên (mật khẩu/email/xoá cứng) | ❌ ¹ | ❌ | ❌ | ✅ tự quản |
| **Lock/suspend account** (`is_active`, reversible) | ✅ | ❌ | ❌ | ❌ |
| Gán lớp, xem dạy trong TT | — | ✅ | ✅ | tham gia |
| Đổi org-role; quản seat | cấp/giới hạn | ✅ | ✅ | — |
| **Tài chính** (hoá đơn / thanh toán) | qua admin-org | ✅ | ❌ ² | — |

¹ **Không có endpoint xoá cứng** cho bất kỳ ai (kể cả admin) — admin chỉ **lock mềm** (`is_active`). Giữ danh tính portable.
² Verified: `OrgGuard.FINANCE_ROLES = {OWNER}` (session 2026-06-22) — MANAGER là org-admin vận hành nhưng **không xem tài chính**.

**Bắt buộc:** mọi endpoint org **lọc theo orgId + kiểm role của caller trong chính org đó**; có test **chống rò chéo tổ chức** (org A không thấy data org B). Ranh giới admin↔giáo-viên-thuộc-TT = **mặc định ẩn + break-glass có audit** (không "no-access" tuyệt đối).

---

## 4. Nâng cấp data model (ngoài materials)

- `OrgMember`: thêm vòng đời **`status` (ACTIVE/REVOKED/LEFT) + joined_at/left_at** (giữ lịch sử, không xoá cứng).
- **SEAT = HỌC VIÊN (đơn vị tính tiền).** `Organization.seat_limit` + `seats_used = số học viên (STUDENT) ACTIVE`; `OrgInvoice.seats` = số học viên. Đây là cách code HIỆN TẠI làm (OrgService/OrgBillingService/AdminOrgService/OrgRosterService) và là mô hình đúng kinh tế (học viên = nơi tiêu quota AI = cost driver). **Giữ nguyên, không đổi.**
- **Giáo viên KHÔNG tiêu ghế billing.** Onboarding giáo viên qua `OrgInvitation` (mời → tham gia; revoke/leave → rời) — chỉ là membership, KHÔNG đụng `seat_limit`/invoice. (Nếu sau này cần chống lạm dụng → thêm **soft cap giáo viên** riêng, không gộp vào seat học viên.)
- `OrgMember`: thêm vòng đời `status` + joined_at/left_at để mời/revoke/self-leave cho giáo viên (độc lập với seat).

---

## 5. Schema TÀI LIỆU (materials) — ĐÃ CHỐT

Một bảng + `owner_scope` tường minh + `CHECK` ép đúng một loại chủ. KHÔNG tách 2 bảng, KHÔNG polymorphic `owner_id`.

```sql
CREATE TABLE materials (
  id           BIGSERIAL PRIMARY KEY,
  owner_scope  VARCHAR(16) NOT NULL,                 -- 'PERSONAL' | 'ORG'
  teacher_id   BIGINT REFERENCES users(id),          -- chủ khi PERSONAL
  org_id       BIGINT REFERENCES organizations(id),  -- chủ khi ORG
  created_by   BIGINT NOT NULL REFERENCES users(id), -- TÁC GIẢ (audit; sống sót khi rời TT)

  title        VARCHAR(255) NOT NULL,
  description  TEXT,
  kind         VARCHAR(16)  NOT NULL,                 -- PPTX | PDF | DOCX | IMAGE ...
  object_key   VARCHAR(512) NOT NULL,                 -- S3 key (không lưu blob)
  mime_type    VARCHAR(120),
  size_bytes   BIGINT,
  visibility   VARCHAR(16)  NOT NULL DEFAULT 'ORG_ALL', -- ORG: ORG_ALL | OWNER_ONLY
  status       VARCHAR(16)  NOT NULL DEFAULT 'ACTIVE',  -- ACTIVE | ARCHIVED | DELETED (soft)
  created_at   TIMESTAMP NOT NULL DEFAULT now(),
  updated_at   TIMESTAMP NOT NULL DEFAULT now(),

  CONSTRAINT chk_material_owner CHECK (
       (owner_scope = 'PERSONAL' AND teacher_id IS NOT NULL AND org_id IS NULL)
    OR (owner_scope = 'ORG'      AND org_id     IS NOT NULL AND teacher_id IS NULL)
  )
);

CREATE INDEX idx_mat_personal ON materials (teacher_id, status, created_at DESC) WHERE owner_scope='PERSONAL';
CREATE INDEX idx_mat_org      ON materials (org_id,     status, created_at DESC) WHERE owner_scope='ORG';
CREATE INDEX idx_mat_author   ON materials (created_by);

-- Gắn tài liệu vào lớp = bảng riêng (KHÔNG nhét class vào materials → giữ sở hữu sạch)
CREATE TABLE class_materials (
  class_id    BIGINT NOT NULL REFERENCES classes(id),
  material_id BIGINT NOT NULL REFERENCES materials(id),
  attached_by BIGINT NOT NULL REFERENCES users(id),
  PRIMARY KEY (class_id, material_id)
);
```

**Bất biến sở hữu:** `CHECK` ép PERSONAL chỉ có `teacher_id`, ORG chỉ có `org_id`. `created_by` chỉ là **tác giả** (audit), KHÔNG quyết định sở hữu → giáo viên tạo tài liệu cho TT thì `owner_scope='ORG'` ngay từ đầu.

**Khi giáo viên rời TT:** dòng PERSONAL theo giáo viên (không đụng); dòng ORG ở lại TT, giáo viên mất quyền vì **truy cập tính theo membership ACTIVE**, không theo `created_by`.

**Truy vấn nóng (mỗi cái 1 partial index, không UNION chéo bảng):**
```sql
-- cá nhân
... WHERE owner_scope='PERSONAL' AND teacher_id=:me AND status='ACTIVE';
-- của TT đang dạy (chỉ chạy khi :me có OrgMember ACTIVE ở :orgId)
... WHERE owner_scope='ORG' AND org_id=:orgId AND status='ACTIVE';
```
Gộp 2 danh sách = `UNION ALL` (mỗi nhánh vẫn dùng index riêng).

**Mở rộng sau (không làm ngay):** `folder_id`/`material_tags`; `visibility='OWNER_ONLY'`; bảng versions.

---

## 6. Endpoint / Schema — trạng thái (cập nhật 2026-06-22, verify từ code)

**✅ ĐÃ CÓ** *(#143 trên `main` — một phần CHƯA deploy prod)*:
- Đổi org-role: `PATCH /api/org/members/{userId}/role` (OWNER-only, MANAGER↔TEACHER).
- Membership: invite giáo viên (`POST /org/teachers/invite`, role MANAGER/TEACHER) · revoke (`DELETE /org/members/{id}`) · teacher self-leave (`POST /org/membership/leave`) — KHÔNG đụng seat.
- Admin org: tạo-org + gắn Owner (`AdminOrgService.attachOwner` — **attach nếu user tồn tại, INVITE nếu email mới**) · list giáo viên tự do (`/api/admin/teachers/free`).
- Seat usage = số học viên (`OrgService.getSeatUsage` — đã đúng "ghế = học viên").
- Materials: CRUD theo scope (V227).
- **Session 2026-06-22 (CHƯA commit):** `POST /api/admin/users` (admin pre-create STUDENT/TEACHER/MANAGER-trong-org/ADMIN) · `PATCH /api/admin/users/{id}/active` (admin lock/suspend) · finance OWNER-only.

**🔧 CÒN THIẾU** *(cho mô hình provisioning top-down §2.1)*:
- **Admin pre-create OWNER:** `POST /admin/users` hiện **từ chối** `orgRole=OWNER` (giữ bất biến 1-OWNER); `attachOwner` khi email mới = **INVITE** chứ không pre-create. ⇒ cần đường **pre-create OWNER** (tạo org + tạo account OWNER + đặt password) **không qua invite**.
- **OWNER/MANAGER pre-create nhân sự (Phase 1, NOW):**
  - **STUDENT ✅ đã có** — CSV import **pre-create account thật** (`/org/students/import` → `OrgRosterService` tạo `User` + membership STUDENT, password ngẫu nhiên UUID; verified).
  - **TEACHER ❌** — hiện chỉ **invite** (account tạo lúc accept); **CHƯA có pre-create TEACHER trực tiếp** (đặt password ngay) cho org-admin.
  - **MANAGER ❌** — chỉ **promote** giáo viên sẵn có (`changeRole`, OWNER-only); không pre-create thẳng.
- **Cột `created_via`** (provenance `ADMIN/OWNER/MANAGER/SELF`): **CHƯA có** (verified: grep rỗng) → cần **additive migration** (nullable + backfill default), set khi tạo; **không ảnh hưởng sở hữu/quyền**.
- **TEACHER self-signup (Phase 2, FUTURE):** CHƯA có — định hướng (endpoint mới `created_via=SELF`, không migrate mô hình).

---

## 7. KHÔNG làm (tránh over-engineer)
Không thêm bảng/cờ "free teacher"; không cho trung tâm **SỞ HỮU** `User` (pre-create/provision thì ĐƯỢC — §2.1/§2.2 — nhưng danh tính vẫn **person-owned & portable**, org không xoá/sửa được); chưa làm đa-trung-tâm-đồng-thời; không nhét class vào bảng materials.

---

## Lịch sử sửa đổi (Revision history)

- **2026-06-22 — Provisioning TOP-DOWN + admin lock + `created_via`** *(chỉ sửa doc; verify từ code, không đụng code).*
  - **Đổi gì:**
    1. **§2 (QĐ #3):** "Admin **mời** Owner tự đăng ký (không pre-create)" → "**PLATFORM-ADMIN pre-create OWNER**" (top-down).
    2. **Thêm §2.1** (chuỗi cấp TK: ADMIN→OWNER, OWNER→{MANAGER,TEACHER,STUDENT}, MANAGER→{TEACHER,STUDENT}; **NOW Phase-1** teacher do org cấp / **FUTURE Phase-2** teacher self-signup = **CHƯA làm**) + **§2.2** (provision ≠ own; teacher portable; lock = platform-admin; student = roster; provenance `created_via`).
    3. **§3 ma trận:** thêm hàng pre-create (OWNER · staff TEACHER/STUDENT · MANAGER=OWNER-only), **Lock/suspend** (platform-admin), **Tài chính** (OWNER-only); chú thích **xoá-cứng chưa có endpoint cho ai**.
    4. **§6:** cập nhật trạng thái thật (#143 + session 2026-06-22) + liệt kê **gap**: admin-pre-create-OWNER · org-admin pre-create TEACHER · cột `created_via` · teacher self-signup.
    5. **§7:** làm rõ "không **SỞ HỮU**" (provision vẫn được).
  - **Lý do:** khớp quyết định user 2026-06-22 (provisioning top-down) + code session này (`POST /api/admin/users`, `PATCH /api/admin/users/{id}/active`, finance OWNER-only). Gỡ mâu thuẫn doc↔code nêu ở **`docs/SESSION_SUMMARY_2026-06-22.md` §E (LỆCH #4)**.
  - **Verify từ code (2026-06-22):** `createUser` role-set = {ADMIN,TEACHER,STUDENT} + orgRole {TEACHER,MANAGER}, **từ chối OWNER**; `AdminOrgService.attachOwner` = attach-nếu-tồn-tại / **invite-nếu-mới**; `OrgGuard.FINANCE_ROLES={OWNER}`; lock = `is_active` `@PreAuthorize hasRole('ADMIN')`; `created_via` **chưa có** (grep rỗng); seat=STUDENT (`OrgService`). **Cần xác minh:** CSV `/org/students/import` tạo account hay invitation.
  - **Portability KHÔNG bị phá:** lock mềm reversible (`is_active`) ≠ xoá danh tính; không endpoint xoá cứng. Không có mâu thuẫn nguyên tắc cần user phân xử.
