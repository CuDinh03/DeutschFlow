import React from 'react';
import PracticePageClient from './PracticePageClient';

export const metadata = {
    title: 'Bài tập bổ trợ | DeutschFlow',
};

export default function PracticePage() {
    return <PracticePageClient />;
}
